"""Dataset implementations.

The core depends on five module attributes: ``NAME``, ``STORAGE``, ``Params``,
``label()``, and ``build()``. Modules may define additional internal helpers and
dataset metadata.
"""
